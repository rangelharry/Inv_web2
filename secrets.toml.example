# Exemplo de arquivo secrets.toml para Streamlit Cloud
# Renomeie para secrets.toml e configure suas variáveis

[database]
# Para SQLite em produção, você pode usar um banco em memória ou arquivo
# Não é recomendado para produção real - use PostgreSQL ou similar
database_url = "sqlite:///inventory.db"

[auth]
# Chave secreta para tokens (gere uma nova chave segura)
secret_key = "sua-chave-secreta-muito-segura-aqui"

[email]
# Configurações de email (opcional)
smtp_server = "smtp.gmail.com"
smtp_port = 587
email_user = "seu-email@gmail.com"
email_password = "sua-senha-de-app"

# Exemplo de como usar no código:
# import streamlit as st
# database_url = st.secrets["database"]["database_url"]
# secret_key = st.secrets["auth"]["secret_key"]